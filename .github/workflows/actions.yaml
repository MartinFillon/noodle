on:
  # pull_request:
  #   branches: [master]
  # push:
  #   branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: test and build
        uses: freckle/stack-action@v5
